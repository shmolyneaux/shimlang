struct Point {
    x,
    y,

    fn add(self, other) {
        Point(
            self.x + other.x,
            self.y + other.y,
        )
    }
}

let p0 = Point(1, 2);
let p1 = Point(2, 3);

let c = p0 + p1;

print("X:", c.x, "Y:", c.y);
