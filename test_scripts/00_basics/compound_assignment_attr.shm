struct Point {
    x, y
}

let p = Point(1, 2);
p.x += 1;
assert(p.x == 2);
p.y -= 1;
assert(p.y == 1);
p.x *= 5;
assert(p.x == 10);
p.x /= 2;
assert(p.x == 5);
p.x %= 3;
assert(p.x == 2);
print(p.x);
print(p.y);
