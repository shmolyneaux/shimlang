struct Container {
    data,
    name,
}

let d = {:}
d.set("key", [1, 2, 3])
let c = Container(d, "test")
let inner_d = {:}
inner_d.set("inner", "value")
let items = [c, Container(inner_d, "other")]
print(items[0].name)
print(items[1].data["inner"])
