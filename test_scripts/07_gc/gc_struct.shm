struct Point {
    x,
    y,
    fn add(self, other) {
        return Point(self.x + other.x, self.y + other.y);
    }
}

let p1 = Point(1, 2);
let p2 = Point(3, 4);
let p3 = p1.add(p2);
print(p3.x);
print(p3.y);
