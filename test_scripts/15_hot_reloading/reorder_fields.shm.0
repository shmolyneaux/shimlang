// Snapshot 0: create a Point and mutate x and z to non-default values.
struct Point {
    x = 1,
    y = 2,
    z = 3,

    fn describe(self) {
        print("\(self.x) \(self.y) \(self.z)")
    }
}

let p = Point()
p.x = 10
p.z = 30

fn loop() {
    p.describe()
}
