// Snapshot 0: create a Player and mutate it to a non-default name.
struct Player {
    name = "hero",

    fn describe(self) {
        print(self.name)
    }
}

let p = Player()
p.name = "renamed"

fn loop() {
    p.describe()
}
