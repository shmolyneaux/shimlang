// Snapshot 1: `helper` is gone and loop no longer calls it. `log` carries over
// as "start-helper" and is extended, proving state survived the removal.
let log = "start"

fn loop() {
    log = log + "-loop"
    print(log)
}
