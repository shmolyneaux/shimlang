struct Rect {
    width = 4,
    height = 3,
    area = width * height,

    fn describe(self) {
        print("\(self.width) x \(self.height) = \(self.area)")
    }
}

let r = Rect()
fn loop() {
    r.describe()
}
