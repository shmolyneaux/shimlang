// Snapshot 0: create a Rect and mutate its width to a non-default value.
struct Rect {
    width = 4,
    height = 3,

    fn describe(self) {
        print("\(self.width) x \(self.height)")
    }
}

let r = Rect()
r.width = 10

fn loop() {
    r.describe()
}
