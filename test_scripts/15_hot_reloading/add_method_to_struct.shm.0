// Snapshot 0: create a Counter and set its value to a non-default 5.
struct Counter {
    n = 0,

    fn show(self) {
        print(self.n)
    }
}

let c = Counter()
c.n = 5

fn loop() {
    c.show()
}
