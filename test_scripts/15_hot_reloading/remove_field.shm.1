// Snapshot 1: the `debug` field is removed. `c` keeps name = "myapp" from
// snapshot 0, proving the instance carried over across the field removal.
struct Config {
    name = "app",

    fn describe(self) {
        print(self.name)
    }
}

let c = Config()

fn loop() {
    c.describe()
}
