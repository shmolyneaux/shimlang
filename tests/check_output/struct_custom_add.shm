struct Point {
    x,
    y

    fn add(self, other) {
        return Point {
            x = self.x + other.x;
            y = self.y + other.y;
        };
    }
}

let a = Point {
    x=2;
    y=3;
};

let b = Point {
    x=3;
    y=4;
};

let c = a + b;
print("c.x =", c.x);
print("c.y =", c.y);
