kind: pipeline
name: default

steps:
- name: test
  image: shmolyneaux/ziglang:0.13.0
  commands:
    - zig build
    - apk add python3
    - cat test_list | xargs ./run_tests

trigger:
  event:
  - push
